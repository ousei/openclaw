# Immutable 目录

此目录用于存储不可变的查询结果快照，便于后续参考和对比。

## 用途

- 存储 Notion API 查询的静态快照
- 保存历史数据，避免重复查询
- 作为数据备份和参考

## 文件命名规范

- `property_inventory.md` - 不动产数据库查询结果
- `todos_latest.md` - 任务数据库查询结果
- 其他查询结果按需命名

## 注意事项

- 此目录中的文件是只读快照，不应被修改
- 每次查询会覆盖同名文件
- 如需保留历史版本，请使用带时间戳的文件名
